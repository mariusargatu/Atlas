"""Tests about the repository and the suite itself: what the CI tiers promise, that the
dependency arrow points one way, that nothing skips silently, and that the one command
runs the whole pipeline against real models and refuses to start without a key."""
